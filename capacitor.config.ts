import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sandwip.blood.donor',
  appName: 'Sandwip Blood Donor BD',
  webDir: 'dist'
};

export default config;
